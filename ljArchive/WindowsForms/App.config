<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <runtime>
    <!-- plugins unpacked from a downloaded zip carry the Internet zone mark, LoadFrom refuses them without this -->
    <loadFromRemoteSources enabled="true"/>
  </runtime>
</configuration>
